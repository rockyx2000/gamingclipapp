# ローカル開発用の環境変数サンプル。コピーして .env.local として使う。
# 動画ファイルと投稿メタデータの保存先ディレクトリ（既定: ./data、apps/web から見た相対パス）
# DATA_DIR=/absolute/path/to/data
# アップロードできる動画ファイルの上限サイズ（MB、既定: 200）
# MAX_UPLOAD_MB=200
# アップロードできるサムネイル画像の上限サイズ（MB、既定: 10）
# MAX_THUMBNAIL_MB=10
